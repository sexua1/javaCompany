<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.mycompany.myapp.dbMapper">

<select id="fetchDb" parameterType="com.mycompany.myapp.DbVo">

  SELECT *
  FROM BOOKS
  WHERE id = #{id}

</select>

</mapper>